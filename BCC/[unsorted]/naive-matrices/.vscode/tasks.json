{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "gcc compile",
            "type": "shell",
            "command": "gcc",
            "args": [
                "main.c", "-o", "naive-matrices.exe", "-g"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}